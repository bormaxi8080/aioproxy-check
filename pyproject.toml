[project]
name = "aioproxy-check"
version = "0.1.0"
description = "Async proxy list checker with optional geolocation and domain checks."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "aiohttp==3.13.5",
    "aiohttp-socks==0.10.1",
    "certifi==2026.2.25",
    "colorama==0.4.6",
    "tqdm==4.67.3",
]

[tool.uv]
package = false
